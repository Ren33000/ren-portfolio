import 'bootstrap';
import Typed from 'typed.js';

if (document.getElementById("animate")) {
  
new Typed('#animate', {
    strings: ["coder", "web-dev", "Front-End developper"],
    typeSpeed: 50,
    loop: true
  });
}

